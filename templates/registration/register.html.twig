{% extends 'layouts/base.html.twig' %}

{% block title %} {{title_url('Inscription')}} {% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            {% for flashError in app.flashes('verify_email_error') %}
                <div class="alert alert-danger" role="alert">{{ flashError }}</div>
            {% endfor %}

            <h1 class="mb-3">Inscription</h1>

            {{ form_start(registrationForm) }}
                <div class="row">
                    <div class="col-6">
                         {{ form_row(registrationForm.firstName, {label:'Prénom'}) }}
                    </div>
                    <div class="col-6">
                        {{ form_row(registrationForm.lastName, {label: 'Nom'}) }}
                    </div>
                </div>
                               
                {{ form_row(registrationForm.email) }}
                {{ form_row(registrationForm.roles) }}
                {{ form_row(registrationForm.plainPassword, {
                    label: 'Mot de passe'
                }) }}
                {{ form_row(registrationForm.agreeTerms) }}

                <button type="submit" class="btn btn-primary mt-2 w-100" formnovalidate>Ajouter</button>
            {{ form_end(registrationForm) }}
        
        </div>
    </div>
{% endblock %}
